name: Self-hosted Runner Smoke Test

permissions: {}

on:
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: self-hosted
    steps:
      - name: Verify self-hosted runner availability
        run: echo "Self-hosted runner is reachable."
